apiVersion: v1
kind: Service
metadata:
  name: seccomp-diff-agent
  namespace: {{ .Values.namespace }}
  labels:
    app: seccomp-diff-agent
spec:
  clusterIP: None
  selector:
    app: seccomp-diff-agent
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
